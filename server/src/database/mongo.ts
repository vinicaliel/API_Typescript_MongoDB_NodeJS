export const mongoClient = {

    async connect(): Promise<void>{

        const url = process.env.MONGOSB_URL
        const username = process.env.MONGODB_USER
        const password =  process.env.MONGODB_PASSWORD

    }
}